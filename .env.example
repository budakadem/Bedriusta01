# Google Analytics 4 Measurement ID (G-XXXXXXXXXX).
# Leave empty and no Google script is ever loaded.
# Create the GA4 data stream against the FINAL domain (bedriusta.de): the ID is
# not bound to the URL entered there, so it keeps collecting — and keeps its
# history — when the site moves off vercel.app.
VITE_GA_MEASUREMENT_ID=
